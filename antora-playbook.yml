site:
  title: story
  url: # how is this effected for local use?
  # start_page: ROOT:index
  #nav:
  #- modules/ROOT/nav.adoc

content:
  sources:
  - url: ./
    branches: HEAD
ui:
  bundle:
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/master/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: true
